type Query {
    login(data: SignInInput): User!
    users: [User]
    user(filter: UserFilter): User
    collaborators: [Collaborator]
    collaborator(filter: CollaboratorFilter): Collaborator
}