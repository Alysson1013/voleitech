type Collaborator_Team {
    id: Int
    collaborator_id: Int
    collaborator: Collaborator
    team_id: Int
    team: Team
    function: String
    status: Boolean   
}

input CollaboratorTeamFilter {
    id: Int
}
