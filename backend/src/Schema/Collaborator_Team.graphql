type Collaborator_Team {
    id: Int
    collaborator_id: Int
    collaborator: Collaborator
    team_id: Int
    team: Team
    function: String
    status: Boolean
    user_id: Int
}

input CollaboratorTeamFilter {
    id: Int
}

input CollaboratorTeamInput {
    team_id: Int
    function: String
    status: Boolean   
}